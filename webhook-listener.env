# webhook-listener.env - 웹훅 리스너 환경 변수
# 이 파일을 서버의 /home/ubuntu/myblog/webhook-listener.env로 복사하고 값을 설정하세요

# 웹훅 수신 포트 (기본값: 9000)
WEBHOOK_PORT=9000

# GitHub 웹훅 시크릿 (GitHub에서 설정한 값과 동일해야 함)
WEBHOOK_SECRET=your-webhook-secret-here

# 프로젝트 디렉토리 절대 경로
PROJECT_DIR=/home/ubuntu/myblog

# Next.js 캐시 무효화 토큰 (임의의 보안 문자열)
REVALIDATE_TOKEN=revalidate-secret-2024
